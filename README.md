thucydides-jbehave-steps
========================

Example to show how mediators variables are initialized once per story.

I need to initialize mediators variables once per scenario. Here is a workaround.
